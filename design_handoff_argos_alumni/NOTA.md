# Carpeta de referencia — no se compila

Contenido de solo lectura. `tsconfig.json` y `.eslintrc.json` la excluyen.

- `README.md` — especificación de diseño, fuente de verdad de la UI.
- `Argos Platform.dc.html` — prototipo de las 4 pantallas. **No copiar el HTML.**
- `Argos Landing.dc.html` — landing pública (Fase 7).
- `support.js` — runtime del prototipo. **No es parte de la entrega: no portar
  ni leer para copiar lógica.**

Los `.dc.html` traen estilos inline y datos mock. Los mocks son contrato de
datos, no contenido: ninguna de sus cifras puede llegar a producción.
